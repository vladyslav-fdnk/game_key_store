[project]
name = "game-key-store"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "aiogram>=3.28.2",
    "asgiref>=3.11.1",
    "django-environ>=0.13.0",
    "djangorestframework>=3.17.1",
]

[tool.uv.workspace]
members = [
    "backend",
]
