[project]
name = "backend"
version = "0.1.0"
description = "Steam Key Store Backend"
requires-python = ">=3.12"
dependencies = [
    "django>=5.0",
    "django-environ>=0.11.2",
    "psycopg2-binary>=2.9.9",
    "djangorestframework>=3.15.1",
]
